[Unit]
Description=Go online the fdu network
After=network.target

[Service]
Type=simple
User=nobody
ExecStart=/usr/bin/go-network -d -c /etc/go-network/config.json

[Install]
WantedBy=multi-user.target